// An action is a plain JavaScript object that describes what happened. It must have a type property and can optionally include additional data (payload).

// Purpose: Describe the event or change that should happen in the application.