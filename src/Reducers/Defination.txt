A reducer is a pure function that takes the current state and an action as arguments and returns a new state.

Purpose: Specify how the state changes in response to actions.


Rules:
        Must not mutate the existing state.
        Must return a new state object.